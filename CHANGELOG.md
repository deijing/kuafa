# 更新日志 (CHANGELOG)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 规范。

## [1.9.0] - 2026-09-02

### 🎨 重点重构与体验升级：口播字幕精修工作台 · 全局字幕开关 · 按素材分切 BGM 伴奏系统

#### 1. 口播字幕精修工作台 (Studio Workshop)
- **React Portal 全局置顶与沉浸式 UI**：彻底消除原有弹窗被侧边栏与顶栏压盖穿透的层级冲突，升级为全屏毛玻璃大圆角 Studio 工作台。
- **1:1 毫秒级短句对齐**：后端采用与 ASS 烧录完全相同的比例字数算法（`expand_to_subtitle_chunks`），字幕列表行与成片画面短句 100% 对应。
- **左侧 9:16 专业真机监看**：内置视频时间轴拖拽、`-2s / +2s` 快进退、`0.75x ~ 1.25x` 倍速、动态发光字幕浮显，支持键盘空格播放/暂停。
- **右侧字幕时间轴编辑**：支持点击每行字幕毫秒级跳转发音位置试听、一键去除末尾标点、批量查找与替换原词、一键保存并重新烧录导出最新成片。

#### 2. 全局字幕独立总开关 (Global Subtitle Switch)
- **设置与生成面板一键开关**：设置中提供「打开字幕」与「关闭字幕（生成纯净无字幕视频）」双卡片选择，偏好全局自动同步。
- **极速纯净出片**：关闭字幕时自动跳过 ASS 烧录渲染阶段，显著缩短视频渲染耗时并输出无字原画成片。

#### 3. 「按素材分切」补齐背景音乐伴奏 (BGM System)
- **全自动轮播 / 自选单曲**：支持在分切工作台自动循环匹配曲库歌曲，或从音乐库下拉选择指定曲目。
- **一键上传音乐至曲库**：支持上传本地音频/视频并自动存入音乐库。
- **精准音量调节**：配备 0~100% 音量滑块，自动平滑混音。

---

## [1.8.0] - 2026-09-02

### 🚀 重点迭代：全局任务实时日志流水 · 云端必剪 ASR 与离线模型检测下载

#### 1. 全局任务实时执行日志流水 (Real-time Task Logs)
- **实时流水弹窗与终端视窗**：在每个成片卡片右上角与状态徽章新增「日志」按钮，点击即可查看任务从排队、ASR 转译、DeepSeek AI 编排、FFmpeg 剪切拼接、字幕烧录到封装出片的完整毫秒级执行日志。
- **流式轮询与自动跟随**：任务运行期间支持 1.2 秒自动增量轮询刷新，日志自动滚屏跟随，并提供一键剪贴板复制全部日志功能。
- **全工作台覆盖**：在「按素材分切」、「批量成片」、「智能成片」与「历史记录」全部视图中均可随时查看各任务流水。

#### 2. 云端必剪 ASR 与本地离线模型一键下载
- **默认切换为云端必剪 ASR**：98%+ 识别准确率，专为中文短视频与直播带货中英混说（如 Polo衫、卫衣、现货秒发等）定制，极速秒转免占本地算力。
- **本地模型智能探测与下载**：设置面板自动扫描本地磁盘中的 Whisper 模型就绪状态，支持一键在后台高速下载并激活模型（如 small/medium/large-v3）。

---

## [1.7.0] - 2026-09-02

### 🚀 重点迭代与特性：侧边栏新增「按素材分切」· 全素材覆盖智能拼接

#### 1. 侧边栏独立功能模块「按素材分切」
- **直观素材勾选与排序**：在素材组中自由勾选多个素材片段，提供视觉化排序序号（① ② ③ ④ ⑤）与动态时长分配提示。
- **三种拼接策略**：支持「所选素材合成 1 条」（100% 覆盖全部素材）、「同素材生成多条差异化版本」或「按每 N 段素材批量打包生成」。

#### 2. 全素材覆盖拼接算法 (build_material_coverage_plan)
- **100% 覆盖保证**：彻底解决按固定总时长贪婪选句时导致后段素材被忽略的问题，对选中的每个素材均分或比例分配时长预算。
- **连贯语义段落抽取**：每个素材独立提取内部语义连贯的话术段落（NarrativeBlock），首素材侧重 Hook 开场，尾素材侧重价格逼单，中间素材聚焦细节卖点。

---

## [1.6.2] - 2026-09-01

### ⚡ 封面规格与成片解耦优化 (Cover 2K & On-demand Generation)

#### 1. 封面画幅规格升级为 9:16 2K 超清
- **9:16 2K 标准规格**：封面工作台、多图融合提示词及生成参数全面更新为 9:16 2K（`1024×1536`）超清商业摄影画质标准，兼顾清晰度与生图效率。

#### 2. 成片封面按需一键生成 (不点不生成)
- **成片与封面彻底解耦**：视频合成阶段不再强制在后台自动调用生图 API，彻底消除不必要的并发消耗与 API 费用。
- **成片预览与历史记录直通一键生成**：成片完成后用户可随时在成片预览页、历史记录或批量卡片中点击「✨ 一键生成 3 张配套爆款封面」按需出图，支持「换一组」重构。

---

## [1.6.1] - 2026-08-21

### ⚡ 成片音画对齐与静音裁切修复 (Audio / Cut Fixes)

#### 1. 说话画面配静音：按同一时间轴裁切音画
- **trim / atrim 精确裁切**：废弃双 `-ss` 关键帧错位，画面与声音按原片同一时间戳裁出，修复「嘴在动、没有声音」的成片静音。
- **有声检测超时不再装没发生**：只解音频、超时杀进程并打日志，避免长切片回退到静音空镜抽样。

#### 2. 成片主路径稳定性
- **补回缺失的密钥读取**：智能混剪 / 批量成片不再因未导入 `get_secret` 启动即失败。
- **批量画质透传**：4K / 2K / 1080P / 720P 选择会真正应用到批量任务。
- **停顿融合与短句保留**：融合缝隙收到 0.20s，真停顿切断，短口播不再被丢掉；封面等待与成片落盘一并加固。

---

## [1.6.0] - 2026-08-21

### 🚀 重点迭代与特性 (Key Features & Enhancements)

#### 1. 双模 ASR 字幕转译引擎 · 本地 Faster-Whisper 离线转写与云端必剪自由切换
- **本地 Faster-Whisper 离线转写引擎**：内置高效 CTranslate2 推理架构与 int8 量化加速，自动适配 CPU 多核与 Apple Silicon 硬件加速，支持中文精准识别与语音活动检测（VAD Filter），彻底解决离线与网络受限环境下的口播识别需求，100% 隐私安全、无任何外部调用限制与费用。
- **云端必剪 ASR 高速转写引擎**：保留必剪云端直连模式，免占用本地算力，极速输出带货口播字幕。
- **设置面板双模切换与规格调节**：在设置中提供「本地离线转译」与「云端必剪转译」自由切换，本地支持 `tiny` / `base` / `small` / `medium` / `large-v3` 多档模型规格选择与一键连通性/延迟测试，配置持久化保存至本机。
- **模型内存常驻与引擎缓存优化**：实现本地 Whisper 实例智能单例与线程安全管理，避免多片段重复加载模型权重；转写缓存按引擎与模型独立归档，切换模式无缝即时生效。

#### 2. 否词筛选增加「不报价格」纯种草模式
- **纯种草与好物开箱定制**：在智能提取规则与否词管理中引入「不报价格」纯种草选项，智能剔除带货口播中含具体金额、券后价、打折促销等营销词段落，专为好物开箱与纯视觉种草成片定制。

### ⚡ 算法与剪辑引擎优化 (Algorithm & Cutting Engine)

#### 1. 音频长停顿贪婪合并修复与消除间歇性无声
- **消除音频吞字与静音空白**：重构音频切片合并逻辑，防止连续长静音区间过度吞并有效语流导致批量成片中间歇性无声。
- **气息微缓冲与平滑淡入淡出**：切片起始/结尾保留毫秒级气息微缓冲与 25ms 平滑淡入淡出，彻底杜绝切点爆音与吞字，音画严格毫秒级同步。

### 🎨 视觉与交互优化 (UI & UX Polish)

#### 1. 批量成片工作台双列大卡片排版
- **一行两列黄金比例布局**：优化批量成片卡片网格为一行两列（1 row 2 cols）大卡片排版，增加最大宽度限制与呼吸感留白，极大提升 3 张配套爆款封面与视频预览的视觉质感。

---

## [1.5.1] - 2026-08-19

### 🎨 视觉与交互重构 (UI & UX Polish)

#### 1. 消息通知与全局弹层层叠上下文（z-index / Stacking Context）深度隔离
- **主视图独立堆叠隔离**：为主内容渲染容器（`<main>` 路由区）添加 `relative z-0 isolate`，将所有子页面的 Sticky 顶部操作栏、带 `backdrop-filter` 磨砂模糊效果的卡片及悬浮元素完全隔离在 `z-index: 0` 独立上下文中。
- **消息提醒层级升级**：将顶部导航栏消息提醒挂载容器升级为 `relative z-50`，彻底解决通知下拉面板被下方页面 Sticky 操作栏与视频卡片穿透、截断的层级错乱问题。
- **全局浮层体系规范**：显式声明 ActiveJobBanner（`relative z-20`）与 ToastContainer（`z-[100]`），理顺整站从底层视图到全局通知的层叠次序。

#### 2. 批量成片工作台卡片微交互优化
- **配套封面缩略图微交互**：优化成片卡片中 3 张配套爆款封面缩略图的网格间距、序号角标与放大镜悬浮预览交互。
- **操作按钮层次强化**：优化成片卡片底部「大屏预览」与「下载 MP4」按钮对比度与排版样式，出片后秒级预览与打包下载更直观。

---

## [1.5.0] - 2026-08-18

### ⚡ 算法与剪辑引擎优化 (Algorithm & Cutting Engine)

#### 1. ASR 智能自然停顿断句与防半截切断保护
- **自然停顿与语流分析**：全面重构 `merge_to_sentences` 算法，依据真实口播静音间隙（`gap >= 0.55s`）与句末语气收尾词（`啦/吧/呢/啊/了/显瘦/透气/包邮` 等）自然断句。
- **未说完连接词防切断保护**：严格避开 `因为/所以/采用/这款/能够/具有` 等未说完的话头截断，彻底解决 ASR 盲目按时长硬截导致的「一句话说到一半就没了」问题。

#### 2. 语义连贯话术段落 (NarrativeBlock) 聚类与黄金叙事结构
- **话术故事单元聚类**：将原片中连续表达的多句话聚类为 6~18 秒的连贯故事段落，段落内部 100% 保持原始口播叙事与语意闭环。
- **爆款带货黄金结构编排**：成片按【Phase 1 开场抓人/新品引出】➔【Phase 2 核心卖点/面料版型/上身细节】➔【Phase 3 破价福利/限时逼单】逻辑自适应组合，批量生成多视频时智能轮换不同开场与卖点组合。

#### 3. DeepSeek / AI 导演级整段精选与编排
- 向 AI 模型输入带完整口播上下文的连贯段落，并在 Prompt 中强制约束「段落必须语意完整、有头有尾」，同时毫秒级对齐成片「神奇大字」弹字动效。

#### 4. 同素材切片无缝融合与音画防爆音缓冲
- **同素材切片融合 (`fuse_adjacent_clips`)**：连续相接的句子直接合并为单次 FFmpeg 连续提取，消除拼接卡顿。
- **防吞字与消除切点爆音**：切片起始/结尾保留毫秒级气息微缓冲，并添加 25ms 音频平滑淡入淡出，彻底杜绝吞字和爆音问题；ASS 字幕与音频时间轴严格毫秒对齐。

---

### ✨ 新增特性 (Features)

#### 1. 背景音乐库 (BGM Library) 独立管理与批量自动轮播
- **专属 BGM 视听管理中心**：支持常见多格式音频（MP3, WAV, AAC, M4A, FLAC, OGG）上传、在线试听、删除与重命名。
- **批量成片智能轮换 BGM**：多视频批量混剪时，自动从音乐库中轮播匹配专属背景音乐，大幅提升批量成片的去重度与观感。

#### 2. 批量混剪与视频预览弹窗
- **智能分组切片缝合**：支持按每 N 段素材自动切片组合生成成片。
- **视频极速弹窗预览**：支持在批量任务和生成记录中直接弹出高清播放器预览成片效果。

---

## [1.4.0] - 2026-08-14

### ✨ 新增特性 (Features)

#### 1. 多张参考图智能融合图生图 (Multi-Reference Image Fusion)
- **支持上传多张参考图**：支持同时上传或选择多张参考图（如：**图 1 实体商品实物/包装细节** + **图 2 主播人物样貌/模特特写**，最多 4 张）。
- **多要素保真融合模型调度**：底层全面接入 GPT Image 2 多图融合参数（`referenceImages`），海报自动将主播与实体商品完美融合在一起，主播手持或展示商品，人物五官与商品细节高度还原。
- **混剪与图生图 100% 同步并行**：批量生成与智能混剪进入选句阶段（35%）即提前提炼口播文案并精选画面，在后台并发启动 AI 图生图，成片完成即封面同步到位（零等待时间）。
- **智能画面美学评分精选引擎**：内置曝光平衡、邻域差分边缘锐度、动态范围对比度与色彩饱和度评分算法，智能过滤闭眼、模糊残影与黑帧，自动挑选最上镜、最高清的主播特写与商品展示帧作为图生图底图。
- **纯粹图生图交互**：彻底移除了冗杂不好看的预设模板与多余样式卡片，界面收敛为「参考底图 ➔ 封面大字文案 ➔ 画幅与张数 ➔ 一键 AI 图生图」。
- **🎙️ 音频智能提炼大字标语**：接入 LLM（DeepSeek / OpenAI）智能分析视频口播录音，一键提炼 3 组紧扣商品名、优惠价格与核心卖点的高转化大字标语。

#### 2. 竖版 9:16 比例与 4K 超高清海报规格
- **竖版 9:16 默认输出**：智能混剪、批量生成与封面工作台出图规格全面升级为标准短视频 **9:16**（`1024×1536`）与 **4K 超高清画质**（`quality="high"`），完美契合抖音、小红书、快手等平台。
- **电商大字报黄金分割提示词体系**：
  - **下半部/右侧**：年轻主播 4K 商业摄影级真实写实抠图特写，五官通透明亮，强负向约束严禁遮挡面部；
  - **上半部/左侧**：吸睛大字报密集促销排版（引流短句刷痕、超大价格数字、核心卖点色块条、圆角卖点标签）。
- **纯粹 AI 视觉模型交付**：彻底移除所有旧版 SVG 模板兜底逻辑，确保每张封面均由真实 AI 视觉模型高质量渲染，杜绝任何假UI或死板矢量模板。

#### 3. 批量制作与智能混剪支持输出画质自由选择
- **4 档画质自由切换**：
  - 🌟 **1080P 全高清** (`1080 × 1920` / CRF 22，推荐默认主流平台标准)
  - 💎 **4K 超高清** (`2160 × 3840` / CRF 17，商业母带级极致清晰)
  - 🎬 **2K 极清** (`1440 × 2560` / CRF 19，影院蓝光级细腻画质)
  - ⚡ **720P 高清** (`720 × 1280` / CRF 26，秒级合成极速出片)
- **前后端渲染管线全链路打通**：前端配置画质参数后，后端 `render_sell_video` 自动匹配计算等比分辨率、CRF 无损度及编码预设（Preset）。

---

### 🎨 界面与体验优化 (UI & UX)
- **自适应画布展示**：封面生成工作台与成片预览区域自动适配 9:16 竖版高清封面展示。
- **大图画廊与操作强化**：支持单张大图放大预览、单张下载高清原图、一键批量下载全部、设为主封面及单张/全部记录删除。

---

## [0.1.0] - 2026-08-13

### 🚀 初始版本发布 (Initial Release)
- **素材库管理**：按一级子文件夹组织素材组，支持拖拽上传、自动读取与缩略图生成。
- **必剪 ASR 语音整句识别**：基于音频断句切割，保障带货口播句子完整不切半字。
- **智能带货混剪**：支持前段商品介绍 + 中后段价格促销结构化拼接。
- **批量成片生成**：从同一素材池一键并发生成多条差异化去重带货成片。
- **SQLite 数据持久化**：任务进度、历史成片与封面记录本地落库，重启不丢失。
